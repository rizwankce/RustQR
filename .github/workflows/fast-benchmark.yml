name: Fast Benchmark

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Target platform'
        required: false
        default: 'macos'
        type: choice
        options:
          - macos
          - linux
          - windows
          - all
      bench_limit:
        description: 'Number of images per category (0 = all)'
        required: false
        default: '25'
        type: string
      category:
        description: 'Optional category filter (e.g. rotations, blurred, high_version)'
        required: false
        default: ''
        type: string

env:
  CARGO_TERM_COLOR: always

jobs:
  benchmark-linux:
    name: Fast Benchmark on linux
    runs-on: ubuntu-latest
    if: ${{ inputs.platform == 'all' || inputs.platform == 'linux' }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-fast-bench-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-fast-bench-
            ${{ runner.os }}-cargo-bench-
            ${{ runner.os }}-cargo-

      - name: Build release (with tools)
        run: cargo build --release --features tools

      - name: Run reading rate benchmark (fast)
        env:
          QR_BENCH_LIMIT: ${{ inputs.bench_limit }}
          QR_MAX_DIM: 1024
        run: |
          echo "## Fast Reading Rate - linux" >> $GITHUB_STEP_SUMMARY
          echo "Limit: \`${{ inputs.bench_limit }}\`" >> $GITHUB_STEP_SUMMARY
          if [ -n "${{ inputs.category }}" ]; then
            echo "Category: \`${{ inputs.category }}\`" >> $GITHUB_STEP_SUMMARY
            for CAT in $(echo "${{ inputs.category }}" | tr ',' '\n'); do
              PROFILE="boofcv-$(echo "$CAT" | tr '_' '-')"
              echo "Running historical profile: $PROFILE" >> $GITHUB_STEP_SUMMARY
              cargo run --features tools --bin qrtool --release -- \
                reading-rate --profile "$PROFILE" --limit "${{ inputs.bench_limit }}" \
                --max-working-dim 1024 --artifact "reading_rate_${CAT}.json" \
                2>&1 | tee "reading_rate_${CAT}.txt"
              echo "---" >> $GITHUB_STEP_SUMMARY
            done
          else
            for CAT in nominal rotations perspective high_version brightness bright_spots lots; do
              PROFILE="boofcv-$(echo "$CAT" | tr '_' '-')"
              echo "Running WP-005 profile: $PROFILE" >> $GITHUB_STEP_SUMMARY
              cargo run --features tools --bin qrtool --release -- \
                reading-rate --profile "$PROFILE" --limit "${{ inputs.bench_limit }}" \
                --max-working-dim 1024 --artifact "reading_rate_${CAT}.json" \
                2>&1 | tee "reading_rate_${CAT}.txt"
              echo "---" >> $GITHUB_STEP_SUMMARY
            done
          fi
          echo '```' >> $GITHUB_STEP_SUMMARY
        shell: bash

      - name: Upload fast benchmark artifacts
        uses: actions/upload-artifact@v4
        with:
          name: fast-benchmark-results-linux
          path: |
            reading_rate_*.txt
            reading_rate_*.json
          retention-days: 14

  benchmark-macos:
    name: Fast Benchmark on macos
    runs-on: macos-latest
    if: ${{ inputs.platform == 'all' || inputs.platform == 'macos' }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-fast-bench-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-fast-bench-
            ${{ runner.os }}-cargo-bench-
            ${{ runner.os }}-cargo-

      - name: Build release (with tools)
        run: cargo build --release --features tools

      - name: Run reading rate benchmark (fast)
        env:
          QR_BENCH_LIMIT: ${{ inputs.bench_limit }}
          QR_MAX_DIM: 1024
        run: |
          echo "## Fast Reading Rate - macos" >> $GITHUB_STEP_SUMMARY
          echo "Limit: \`${{ inputs.bench_limit }}\`" >> $GITHUB_STEP_SUMMARY
          if [ -n "${{ inputs.category }}" ]; then
            echo "Category: \`${{ inputs.category }}\`" >> $GITHUB_STEP_SUMMARY
            for CAT in $(echo "${{ inputs.category }}" | tr ',' '\n'); do
              PROFILE="boofcv-$(echo "$CAT" | tr '_' '-')"
              echo "Running historical profile: $PROFILE" >> $GITHUB_STEP_SUMMARY
              cargo run --features tools --bin qrtool --release -- \
                reading-rate --profile "$PROFILE" --limit "${{ inputs.bench_limit }}" \
                --max-working-dim 1024 --artifact "reading_rate_${CAT}.json" \
                2>&1 | tee "reading_rate_${CAT}.txt"
              echo "---" >> $GITHUB_STEP_SUMMARY
            done
          else
            for CAT in nominal rotations perspective high_version brightness bright_spots lots; do
              PROFILE="boofcv-$(echo "$CAT" | tr '_' '-')"
              echo "Running WP-005 profile: $PROFILE" >> $GITHUB_STEP_SUMMARY
              cargo run --features tools --bin qrtool --release -- \
                reading-rate --profile "$PROFILE" --limit "${{ inputs.bench_limit }}" \
                --max-working-dim 1024 --artifact "reading_rate_${CAT}.json" \
                2>&1 | tee "reading_rate_${CAT}.txt"
              echo "---" >> $GITHUB_STEP_SUMMARY
            done
          fi
          echo '```' >> $GITHUB_STEP_SUMMARY
        shell: bash

      - name: Upload fast benchmark artifacts
        uses: actions/upload-artifact@v4
        with:
          name: fast-benchmark-results-macos
          path: |
            reading_rate_*.txt
            reading_rate_*.json
          retention-days: 14

  benchmark-windows:
    name: Fast Benchmark on windows
    runs-on: windows-latest
    if: ${{ inputs.platform == 'all' || inputs.platform == 'windows' }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-fast-bench-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-fast-bench-
            ${{ runner.os }}-cargo-bench-
            ${{ runner.os }}-cargo-

      - name: Build release (with tools)
        run: cargo build --release --features tools

      - name: Run reading rate benchmark (fast)
        env:
          QR_BENCH_LIMIT: ${{ inputs.bench_limit }}
          QR_MAX_DIM: 1024
        run: |
          echo "## Fast Reading Rate - windows" >> $GITHUB_STEP_SUMMARY
          echo "Limit: \`${{ inputs.bench_limit }}\`" >> $GITHUB_STEP_SUMMARY
          if [ -n "${{ inputs.category }}" ]; then
            echo "Category: \`${{ inputs.category }}\`" >> $GITHUB_STEP_SUMMARY
            for CAT in $(echo "${{ inputs.category }}" | tr ',' '\n'); do
              PROFILE="boofcv-$(echo "$CAT" | tr '_' '-')"
              echo "Running historical profile: $PROFILE" >> $GITHUB_STEP_SUMMARY
              cargo run --features tools --bin qrtool --release -- \
                reading-rate --profile "$PROFILE" --limit "${{ inputs.bench_limit }}" \
                --max-working-dim 1024 --artifact "reading_rate_${CAT}.json" \
                2>&1 | tee "reading_rate_${CAT}.txt"
              echo "---" >> $GITHUB_STEP_SUMMARY
            done
          else
            for CAT in nominal rotations perspective high_version brightness bright_spots lots; do
              PROFILE="boofcv-$(echo "$CAT" | tr '_' '-')"
              echo "Running WP-005 profile: $PROFILE" >> $GITHUB_STEP_SUMMARY
              cargo run --features tools --bin qrtool --release -- \
                reading-rate --profile "$PROFILE" --limit "${{ inputs.bench_limit }}" \
                --max-working-dim 1024 --artifact "reading_rate_${CAT}.json" \
                2>&1 | tee "reading_rate_${CAT}.txt"
              echo "---" >> $GITHUB_STEP_SUMMARY
            done
          fi
          echo '```' >> $GITHUB_STEP_SUMMARY
        shell: bash

      - name: Upload fast benchmark artifacts
        uses: actions/upload-artifact@v4
        with:
          name: fast-benchmark-results-windows
          path: |
            reading_rate_*.txt
            reading_rate_*.json
          retention-days: 14
