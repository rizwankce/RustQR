[package]
name = "rust_qr"
version = "0.1.0"
edition = "2024"
description = "RustQR rebuild scaffold"
license = "MIT OR Apache-2.0"

[features]
default = []
tools = []

[dependencies]
image = { version = "0.24.9", default-features = false, features = ["png", "jpeg", "gif", "bmp"] }
quircs = "0.10"
